

In Java 2D Array matrix implementation comes with wastage of memory.some people use
arrays of array to create multi dimension array, its well memory utilizable.


  public int[][] printArray(int[][] matrix) {
       for(int i=0;i<matrix.length;i++){
           for (int j=0;j<matrix[i].length;j++) {
               System.out.println("matrix["+i+"]["+j+"] : " + matrix[i][j]);
           }
        }
        return new int[][] {};
    }